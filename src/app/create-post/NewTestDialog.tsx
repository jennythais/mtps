import React, { forwardRef } from 'react'
import { useForm } from 'react-hook-form'

const NewTestDialog = forwardRef((_, ref) => {
  const { } = useForm({

  })
  return (
    <div>NewTestDialog</div>
  )
})

export default NewTestDialog